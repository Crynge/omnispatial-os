{
  "name": "omnispatial-os",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "build:web": "npm --prefix frontend-dashboard run build",
    "lint:web": "npm --prefix frontend-dashboard run lint",
    "audit": "python -m pytest tests -q && python -m compileall services src tests && npm --prefix frontend-dashboard run build"
  }
}

